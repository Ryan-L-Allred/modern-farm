//Define a function called createAsparagus and have it return an object with the appropriate properties. 
//Assign the object to a variable with a nice descriptive name so that the function can return the object.
//Import the code from this module to test my logic
//These algorithms essentially apply to the rest of the seeds modules, except for corn where it returns an array of two objects.
//Use export keyword in front of the functions.

export const createAsparagus = () => {
    const asparagusObject = {
        type: "Asparagus",
        height: 24,
        output: 4
    }
return asparagusObject
}